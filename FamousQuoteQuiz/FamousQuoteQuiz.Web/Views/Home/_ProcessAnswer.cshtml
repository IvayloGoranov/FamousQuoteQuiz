@using FamousQuoteQuiz.Web.Controllers
@model string

<div class="row">
    <div class="col-md-12 text-center">
        <p class="text-muted">
            @Model
        </p>
    </div>
</div>

<div class="row">
    <div class="col-md-12 text-center">
        @(Html.ActionLink<HomeController>("Next", c => c.Index(), null, new { @class = "btn btn-success" }))
    </div>
</div>